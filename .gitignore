node_modules/
dist/
*.tsbuildinfo
.DS_Store
*.log
coverage/
.idea/

# Local-only agent / scratch dirs. Do not publish briefs.
.claude/
.local/

# Secrets. This repo has no required env for the default path.
.env
.env.*
*.pem

# Build artifact: the single-file export bundle.
# Rebuild with `pnpm -F @codeville/atlas-ui build`.
apps/web/public/standalone/

# Generated atlases embed the source excerpts of whatever repo you point at.
# Regenerate with `pnpm atlas <path>` rather than committing someone's codebase.
atlases/

# Explanation cache (lives in ~/.cache/codeville, but never commit a stray one).
.codeville/
